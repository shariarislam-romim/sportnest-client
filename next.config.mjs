/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  reactCompiler: true,
  reactCompiler: true,
  images: {
    domains: [
      "images.unsplash.com",
      "upload.wikimedia.org",
      "i.pravatar.cc"
    ],
  },
};

export default nextConfig;
